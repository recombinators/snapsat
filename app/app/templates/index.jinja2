{% extends "base.jinja2" %}

{% block content %}

<header class='contain'>
    <h1 class='mbn'><a href='/'>Snapsat</a></h1>
    <p class='blurb mbl'>The easiest way to create custom Landsat band composites.</p>
    <p class='light'>The Landsat 8 satellite gives us a way to see outside the spectrum of light we're typically accustomed to seeing. It's a means of seeing what would otherwise be invisible. And it does so on a global scale. AKA superpowers. If you're already up to date on how Landsat can give you useful information, continue on, otherwise, <a href='/guide'>check out our guide.</a></p>
</header>

<section id='where' class='mbx'>
    <h2 class='contain'>
        <span class='lead'>1</span> 
        Select a location
    </h2>
    <div class='map-container'>
        <div id='map'>
            <span class="reticle bold red h3"><img src='/static/logo.svg'></span>
        </div>
    </div>
</section>

<section class='contain mbx'>
    <h2>
        <span class='lead'>2</span>
        Select a date
    </h2>
    <div id='pathrowgrouping'>
    {% for group in scenes_path_row %}
        <div>
            <h3 class='mvm'>{{ group[0].sliced }}</h3>
            <table>
                <thead>
                    <tr>
                        <th>Date acquired</th> <th>Path</th> <th>Row</th> <th>Cloud cover</th> <th>ID</th>
                    </tr>
                </thead>
                <tbody>
                        {% for s in group %}
                            <tr>
                                <td>{{ s.acquisitiondate }}</td>
                                <td>{{ s.path }}</td>
                                <td>{{ s.row }}</td>
                                <td>{{ s.cloudcover }}</td>
                                <td><a href='/scene/{{ s.entityid }}'>{{ s.entityid }}</a></td>
                            </tr>
                        {% endfor %}
                </tbody>
            </table>
        </div>
    {% endfor %}
    </div>
</section>

<section class='contain mbx'>
    <h2>
        <span class='lead'>3</span>
        There is no step three!
    </h2>
    <p class='light'>We hope you enjoyed Snapsat. If you're looking to learn more about what's possible, <a href='/guide'>visit our guide</a>. If you build something awesome with it, let us know!</p>
    <p class='light'>Snapsat was built by <a href=''>Constantine</a>, <a href=''>Mark</a>, <a href=''>Jake</a>, <a href=''>Joel</a>, and <a href=''>Jacques</a> at <a href=''>CodeFellows</a>.</p>
    <p class='light'>We'd love to hear from you!</p>
</section>

</footer>

{% endblock %}
